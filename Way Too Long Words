
#include <stdio.h>
#include <string.h>
main()
{
    char a[50];
    int i,n;
    gets(a);
    n=strlen(a);
    if(n<=10)
        puts(a);
    else
    {
        i=n-2;
        printf("%c%d%c",a[0],i,a[n-1]);
    }


}
